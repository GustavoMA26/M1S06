// No módulo somaArray.js, exporte uma função chamada somaArray, que recebe um array de números como parâmetro e retorna a soma de todos os elementos.

export default function somaArray (n1, n2) {
    return n1 + n2;
}

somaArray (5,3);
