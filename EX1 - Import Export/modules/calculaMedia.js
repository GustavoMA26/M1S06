// No módulo calculaMedia.js, exporte uma função chamada calculaMedia, que recebe um array de números como parâmetro e retorna a média aritmética desses números.

export default function calculaMedia (n1, n2) {
    return ((n1 + n2)/2);
}

calculaMedia (5,3);